ng-ui angular (analytics-UI demo)
rct-ui react (analytics-UI)
data-gen-kfk data-gen-to-kafka (data generation)
agg-kfk-producer consumer-agg-producer (data aggregation)
agg-kfk-consumer-ws metrics-consumer (analytics)



## PySpark

- open spark shell and print version

```shell
spark-shell
sc.version // For Scala
```
type `:quit:` to quit


- open py spark shell and print version
```
pyspark
print(sc.version) # For Python
```

